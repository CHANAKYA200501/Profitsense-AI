import React from 'react';
import { AdminPanel } from './AdminPanel';

/**
 * AdminApp — rendered at /admin, protected by the main App routing.
 */
export const AdminApp: React.FC = () => {
  return <AdminPanel />;
};
